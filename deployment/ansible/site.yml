- name: Build
  hosts: 127.0.0.1
  connection: local
  vars_files:
    - vars.yml
  roles:
    - build_dw
#    - build_back ?
#    - build_front

- name: Deploy
  hosts: prod_servers
  become: true
  become_user: "{{ maintainer_user }}"
  vars_files:
    - vars.yml
  roles:
    - files
    - docker
    - deploy_dw
#    - deploy_back
#    - deploy_front
#    - apache
